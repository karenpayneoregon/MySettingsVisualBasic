Imports System.Configuration

Public Class Form1
    Private Sub Button1_Click(sender As Object, e As EventArgs) _
    Handles Button1.Click


    End Sub
    '
    ' This example there is no code, everything is done via (ApplicationSettings)/(PropertyBinding)
    ' for the TextBox.Text property.
    ' 
End Class
